apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pubsub-operator
  labels:
    app: pubsub-operator
rules:
- apiGroups:
  - pubsub.k8s.io
  resources:
  - ResourceAll
  verbs:
  - VerbAll